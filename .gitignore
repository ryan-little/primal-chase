# Claude Code
CLAUDE.md
.claude/
.mcp.json

# Internal tooling
# NOTE: test/charts.html, test/report.js and test/simulate.js are intentionally
# tracked. Blanket-ignoring test/ meant any *new* file added there was silently
# skipped by `git add`. Ignore the noisy output instead, not the whole directory.
test/out/
test/results/
docs/

# Dependencies (dev only — vanilla JS game, no runtime deps)
node_modules/
package.json
package-lock.json

# Playwright artifacts
/*.png
/*.jpeg
.playwright-mcp/

# macOS
.DS_Store
